source 'https://rubygems.org'

gem 'sinatra',         '~> 3.0'
gem 'sinatra-contrib', '~> 3.0'   # sinatra/json, sinatra/reloader
gem 'puma',            '~> 6.0'   # web server
gem 'ostruct'                      # required by rack on Ruby 4.0+

# Database
gem 'mysql2',   '~> 0.5'
gem 'sequel',   '~> 5.75'        # ORM / query DSL on top of mysql2

# Auth & Security
gem 'bcrypt',   '~> 3.1'
gem 'jwt',      '~> 2.7'

# File uploads
gem 'cloudinary', '~> 1.28'

# Environment & CORS
gem 'dotenv',    '~> 2.8'
gem 'rack-cors', '~> 2.0'

# Email delivery via Resend HTTP API (uses stdlib net/http — no extra gem needed)
